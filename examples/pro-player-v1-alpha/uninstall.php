<?php
/**
 * Uninstall handler for Music 4 Robots 2 Dance 2.
 *
 * This plugin persists no options, custom tables, or post meta — it only
 * registers shortcodes, a block, and filters at runtime. Uninstall is therefore
 * a deliberate no-op (nothing to clean up). The file exists so WordPress runs a
 * controlled path on delete rather than guessing.
 *
 * @package music4robots2dance2
 * License: GPL-3.0-or-later
 */

if ( ! defined( 'WP_UNINSTALL_PLUGIN' ) ) {
    exit;
}

// Nothing stored; nothing to remove. take · own · use · share.
